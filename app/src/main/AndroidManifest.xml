<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.plateform.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestAssignment">
        <activity
            android:name="com.example.plateform.core.ui.PdfReport"
            android:exported="false" />
        <activity
            android:name="com.example.plateform.core.ui.FormActivity"
            android:exported="false" />
        <activity
            android:name="com.example.plateform.core.ui.CustomerListActivity"
            android:exported="false" />
        <activity
            android:name="com.example.plateform.core.ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>